VITE_APP_BASE_URL=https://study.sanghak.kr

# Cloudflare Pages Function secrets (server-side only, never exposed to client):
# wrangler pages secret put OPENAI_API_KEY --project-name study
# wrangler pages secret put GOOGLE_CHAT_WEBHOOK_URL --project-name study
# Optional:
# wrangler pages secret put OPENAI_MODEL --project-name study
